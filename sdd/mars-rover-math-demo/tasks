# Tasks: Mars Rover Math Demo (v1)

## Phase 1: Foundation (dominio y contratos)

- [x] 1.1 Crear `src/domain/rover/types.js` con shape runtime de `Plateau`, `State`, `Command`, `Warning`, `DomainError` y códigos (`MOVE_OUT_OF_BOUNDS`, `ROVER_OVERLAP_ALLOWED`).
- [x] 1.2 Crear `src/domain/rover/validate.js` con `validatePlateau`, `validateState`, `validateCommands` y errores explícitos de rango/orientación/comando.
- [x] 1.3 Crear `src/domain/rover/transform.js` con `turnLeft`, `turnRight`, `moveForward` puros e inmutables.
- [x] 1.4 En `moveForward`, aplicar política v1: si sale de `[0..Xmax]×[0..Ymax]`, mantener estado y devolver warning estructurado.

## Phase 2: Motor secuencial por rover

- [x] 2.1 Crear `src/domain/rover/execute.js` con `executeCommand(state, cmd, plateau)` que retorne `{nextState, warning?}`.
- [x] 2.2 Implementar `executeRoverSequence(initial, commands, plateau)` con fold izquierda→derecha y traza por paso `{stepIndex, cmd, prev, next, warning?}`.
- [x] 2.3 Validar en `executeRoverSequence` que comandos inválidos no rompan silenciosamente: error tipado o rechazo previo.
- [x] 2.4 Agregar tests unitarios de dominio (nuevo `tests/domain/rover/*.test.js`) para L/R/M, borde inmóvil con warning, y caso PRD `LMLMLMLMM => (1,3,N)`.

## Phase 3: Parser y orquestación multi-rover

- [x] 3.1 Crear `src/application/parser.js` para texto de escenario (plateau + bloques rover) con salida `{ok,value}` o `{ok:false,errors[]}`.
- [x] 3.2 Implementar catálogo de errores parser (`PARSE_PLATEAU_FORMAT`, `PARSE_ROVER_BLOCK_INCOMPLETE`, `PARSE_ORIENTATION_INVALID`, `PARSE_COMMAND_INVALID`, `PARSE_COORD_OUT_OF_RANGE`) con línea y `roverIndex`.
- [x] 3.3 Crear `src/application/simulateScenario.js` que procese rovers en orden de entrada (A completo, luego B), generando `finals`, `roverTraces`, `timeline` global.
- [x] 3.4 Incluir warning `ROVER_OVERLAP_ALLOWED` cuando corresponda en inicio/fin de rover sin bloquear simulación.
- [x] 3.5 Agregar tests de integración (`tests/application/simulateScenario.test.js`) para orden global A→B, completitud de timeline y parse válido/inválido.

## Phase 4: UI Phaser + feedback operativo

- [x] 4.1 Crear `src/ui/phaser/RoverScene.js` para grilla cartesiana, rover activo y path acumulado por frame.
- [x] 4.2 Crear `src/ui/phaser/playbackAdapter.js` para consumir `timeline` con controles `play/step/pause/reset` y velocidad.
- [x] 4.3 Crear `src/ui/statePanel.js` para estado actual `S=(x,y,θ)`, listado de traza y badge visible de warnings por fila.
- [x] 4.4 Modificar `index.html` con layout responsive (input escenario, botones run/play/step/reset, panel de errores/warnings).
- [x] 4.5 Modificar `game.js` para bootstrap del flujo: parsear input, bloquear play/step si `ok=false`, y cablear adapter + panel.

## Phase 5: Validación manual mínima v1

- [x] 5.1 Ejecutar caso feliz de 2 rovers del PRD y verificar finales + timeline en UI.
- [x] 5.2 Ejecutar caso de borde (`S` en `0,0,S` + `M`) y verificar inmovilidad + warning visible en timeline.
- [x] 5.3 Ejecutar input malformado (bloque incompleto y comando inválido) y verificar bloqueo de Play/Step + errores con línea/código.
- [x] 5.4 Verificar usabilidad mínima en desktop y viewport mobile (sin desborde de paneles ni pérdida de controles).

## Phase 6: Remediación post-verify (gaps críticos)

- [x] 6.1 Agregar test explícito para escenario spec `Invalid coordinate outside plateau`.
- [x] 6.2 Agregar test explícito para escenario spec `Orientation domain enforcement`.
- [x] 6.3 Agregar test explícito parser para `PARSE_ROVER_BLOCK_INCOMPLETE`.
- [x] 6.4 Agregar evidencia automatizada para `State panel synchronization`.
- [x] 6.5 Agregar evidencia automatizada para `Boundary warning surfaced`.
